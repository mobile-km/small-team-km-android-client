<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:title="设置"
        >
<!--     <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        style="@style/head_bar"
        android:id="@+id/head_bar"
    >
        <ImageView
            android:id="@+id/iv_foot_view"
            android:src="@drawable/sliding_menu"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:contentDescription="@string/app_name"
        />
        <TextView
            style="@style/head_bar_text_center"
            android:text="@string/chat_title"
        />
    </RelativeLayout>    --> 
    
    <ListPreference
            android:entries="@array/upload_photo_quality_entry"
            android:entryValues="@array/upload_photo_quality_value"
            android:title="上传图片质量"
            android:key="@string/preferences_key_upload_photo_quality"
            android:summary="发送主题时，上传的图片的质量"
            android:defaultValue="0"
    >    
    </ListPreference>

</PreferenceScreen>
